# nix-starter pre-commit hooks.
# Formats all .nix files with nixfmt before each commit.
repos:
  - repo: local
    hooks:
      - id: nixfmt
        name: nixfmt
        entry: nixfmt
        language: system
        types: [nix]
